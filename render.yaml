services:
  - type: web
    name: ai-sql-copilot-api
    env: python
    plan: free
    rootDir: .
    buildCommand: python3 -m pip install -r requirements.txt
    startCommand: uvicorn src.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /healthz
    autoDeploy: true
